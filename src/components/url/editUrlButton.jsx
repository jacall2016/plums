'use client'
import React from 'react';

function EditUrlButton({ onClick }) {
    return (
      <button onClick={onClick}>
        Edit URL
      </button>
    );
  }
  
  export default EditUrlButton;