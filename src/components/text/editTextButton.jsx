'use client'
import React from 'react';

function EditTextButton({ onClick }) {
    return (
      <button onClick={onClick}>
        Edit Text
      </button>
    );
  }
  
  export default EditTextButton;